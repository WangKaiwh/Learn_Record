
echo -n "xxxx"
echo -n的作用是显示一行字符串，但是并不在末尾显示换行。
当输入一串数字后，回车再换行。

read -p "xxxx" name
-p 先输出一段xxxx，但不回车，当输入完毕后(回车)，name被赋值。

read -n x val
-n 会读取指定长度的字符，当字符的长度达到x时候，自动将值赋值给val，并不需要回车。

read -t x
-t 会指定x秒，当超过x秒后，用户没有输入，则返回一个状态非0的状态。

read -s
-s 会隐藏输入的字符串。
